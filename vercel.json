{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd web && corepack enable && corepack prepare pnpm@10.33.2 --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd web && pnpm build",
  "outputDirectory": "web/.next"
}
