{
  "buildCommand": "pnpm build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_BACKEND_URL": "@pulse_backend_url"
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://pulse-backend.up.railway.app/api/:path*"
    }
  ]
}
